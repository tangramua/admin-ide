export const GIT_API_NAMESPACE = '@git-api';

export const ID_TOKEN = 'id_token';

// routes
export enum GIT_API_ROUTES {
    GIT_REPOSITORIES = '@git-api/repositories',
    GIT_REPOSITORY = '@git-api/repository',
}

