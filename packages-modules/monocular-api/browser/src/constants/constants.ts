export const MONCOULAR_NAMESPACE = '@monocular-api';

// Routes
export enum MONOCULAR_ROUTES {
    CHART = '@monocular-api/chart',
    CHARTS = '@monocular-api/chart/charts',
    REPO_CHART = '@monocular-api/chart/:registry/:repo/:chart',
}
