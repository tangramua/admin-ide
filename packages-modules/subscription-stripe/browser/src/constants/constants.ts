
export const SUBSCRIPTION_STRIPE_NAMESPACE = '@subscription-stripe';


export enum SUBSCRIPTION_ROUTES {
    BILLING = '@subscription-stripe/billing',
    PAYMENT = '@subscription-stripe/payment',
    REPORT = '@subscription-stripe/report',
}
