mutation UpdateWorkspace($request: WorkspaceUpdate_Input!) {
    updateWorkspace(request: $request) {
        id
        name
        language
        status
        description
        icon_url
        spec {
            cpu
            ram
            hdd
        }
    }
}